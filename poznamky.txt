https://www.gymrk.sk/Direr/pascal/index.htm#ucivo
	Jednoduché príkazy
	príkaz - povel na vykonanie èinnosti
	vstup("read", "readln()"), vıstup("write()", "writeln()"), priradenie(":="), skok("goto"), volanie procedúry a prázdny príkaz

Zloené príkazy
	cykly(for, while, repeat)

	for i:=1 ("to","downto") 7 do
	begin
		sth
	end;

	while "podmienka" do
	begin
		sth
	end;

	repeat
		sth
	until "podmienka";

viacnásobné vetvenie(case)
	case "hladanı objekt" of
		1: príkaz;
		2..10: príkaz;
		11,15,26: begin
				príkaz1;
				príkaz2;
			  end;
	else príkaz; //nepovinné
	end;

shortstring - od 0 po 255 znakov,

delete("reazec z ktorého","ktorá pozícia","ko¾ko miest")
insert("vkladanı podreacez","reazec do ktorého sa vkladá","pozícia v tomto reazci")
pos("h¾adanı reazec","v ktorom reazci h¾adá")
copy("z ktorého reazca sa bude kopírova","od akej pozície v tomto reazci","ko¾ko miest od tejto pozície")

chr(64) vráti znak pre hodnotu 64(@)
ord(A) vráti hodnotu pre znak A(65) 


Math
	Max(hodnota1,hodnota2) //Nájde najväèšiu hodnotu z tıchto dvoch
	Min(hodnota1,hodnota2)
	MaxValue(pole) //Nájde najväèšiu hodnotu v poli
	MinValue(pole)

SysUtils
	IntToStr()
	StrToInt()

crt
	textcolor()
	clrscr
	gotoxy()
	keypressed
	readkey()
	textBacground()

#77-vpravo
#75-vlavo
#72-hore
#80-dole
#27-ESC
http://wiki.freepascal.org/ASCII

vıraz - aritmetické(vısledok je èíslo), relaèné(boolean), logické
	-premenné, konštaty, okrúhle zátvorky, funkcie, aritmetické operátory

priorita operátorov - not, unárne mínus, multiplikatívne operátory(krát, delenie, div, mod, log. súèin), aditívne(+, -, or xor), relaèné(<,>,<>,<=,>=,=)

konjunkcia,sekvencia(bez if)
disjunkcia(vetvenie, iba jenda  vetva)
repetiènı(nieko¾ko násobné opakovanie)

assignFile(internı nazov,nazov dokumentu);
append(internı nazov);  //otvorí existujúci dokumnet a doplní
reset(internı nazov);   //otvorí existujúci dokumnet a èíta
rewrite(internı nazov); //vytvorí dokumnet
closeFile(internı nazov);

function 'názov' (formalne premenné:údajovı typ):údajovı typ;
var lokálne premenné
begin
  'názov':=
end;

Typy súborov
	Textovı (èkolo¾vek), uknoèenia riadkov aj súboru (read,write,readln,writeln)
	Typové (iba jeden typ v òom) ako pole, iba ukunèenie súboru (read,write) èísluje sa od 0!! seek(var F; n:longint)-presunie na zadané poradové èislo dátového objektu, filePos(F)-aktuálna pozícia, FileSize(F)-poèet objektov v súbore, teumcate(F)vymae všetko od pozície kde som
	Netypové (poloky rôzneho typu)